---
import { formatDate } from '../utils';
interface Props {
  date: Date;
  class?: string;
}
const { date, class: className } = Astro.props;
---

<time class={className} datetime={date.toISOString()}>{formatDate(date)}</time>
